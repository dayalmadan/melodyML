import { createTheme } from '@mui/material/styles';

export const sliderTheme = createTheme({
  palette: {
    primary: {
      main: '#29434e',
    },
    secondary: {
      main: '#29434e',
    },
  },
});
